import { defineConfig } from 'vitest/config';

/** Re-export: prefer `cd midnight-contract && npx vitest run --config vitest.rise.config.ts` */
export { default } from './midnight-contract/vitest.rise.config.ts';
